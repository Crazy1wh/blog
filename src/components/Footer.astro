<footer>
	<div class="footer-inner">
		<p>&copy; 2026 51QDD Blog</p>
		<nav aria-label="Footer navigation">
			<a href="https://51qdd.com" target="_blank" rel="noopener noreferrer">主站：51qdd.com</a>
			<span>隐私政策</span>
			<span>免责声明</span>
		</nav>
	</div>
</footer>
<style>
	footer {
		border-top: 1px solid var(--border);
		background: #fff;
		color: var(--muted);
		font-size: 0.94rem;
	}
	.footer-inner {
		display: flex;
		align-items: center;
		justify-content: space-between;
		width: min(100% - 32px, 960px);
		margin: 0 auto;
		padding: 32px 0;
		gap: 20px;
	}
	p {
		margin: 0;
	}
	nav {
		display: flex;
		flex-wrap: wrap;
		justify-content: flex-end;
		gap: 14px;
	}
	a {
		color: var(--muted);
		text-decoration: none;
	}
	a:hover {
		color: var(--text);
	}
	@media (max-width: 640px) {
		.footer-inner {
			align-items: flex-start;
			flex-direction: column;
		}
		nav {
			justify-content: flex-start;
		}
	}
</style>
